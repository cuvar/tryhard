<!DOCTYPE html>
<html lang="en">
<head>
  <title>{{ title }}</title>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta
    name="description"
    content="Hey there, welcome on my blog! I write about everything that comes to my mind or interests me - whether it's about tech, sports, science, just random thoughts or other topics."
  />
  <script src="https://cdn.tailwindcss.com"></script>
  <link
      href="https://fonts.googleapis.com/css2?family=Noto+Sans:wght@400;600&display=swap"
      rel="stylesheet"
    />
  <script>
    tailwind.config = {
      theme: {
        extend: {
          fontFamily: {
            'sans': '"Noto Sans"'
          },
        },
      }
    }
  </script>
</head>
<body>
  <div class="min-h-screen">
  {% include "./components/navbar.njk" %}
  <main class="min-h-screen h-full mx-10 my-10 flex justify-center">
    <div class="w-10/12 sm:w-8/12 md:w-8/12 lg:w-6/12 xl:w-4/12">
      <ul>
        {% for post in search.pages("post") %}
        <li class="mb-8">
          <a href="{{post.data.url}}" class="text-2xl font-bold">
          {{ post.data.title }}
          </a>
          <div class="text-gray-500">
            {{ post.data.created }}
          </div>
          <div class="text-gray-800 mt-2">
            {{ post.data.summary }}
          </div>
        </li>
        {% endfor %}
      </ul>
    {# {{ content | safe }} #}
    </div>
  </main>
  {% include "./components/footer.njk" %}
  </div>
</body>
</html>